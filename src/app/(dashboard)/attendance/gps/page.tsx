import { PlaceholderPage } from "@/components/layout";

export default function GpsCheckInPage() {
  return (
    <PlaceholderPage
      title="GPS Check-In / Check-Out"
      description="Guard location-based attendance will be handled here."
    />
  );
}

export const metadata = {
  title: "GPS Check-In | Security ERP",
};

import { PlaceholderPage } from "@/components/layout";

export default function SettingsPage() {
  return <PlaceholderPage title="Settings" />;
}

export const metadata = {
  title: "Settings | Security ERP",
};
